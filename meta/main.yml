---
galaxy_info:
  role_name: ansible-role-mongoc
  author: alikarimii
  description: local mongo cluster
  company: Software Genesis Group
  license: "license (BSD, MIT)"
  min_ansible_version: 2.1
  github_branch: main
  github_repo: https://github.com/SGGC-CO/ansible-role-mongoc.git
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags: [orchestration, mongo, database, cluster]
dependencies:
  []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
